# 09.07 · fragments

> _what survived compression. fragments worth keeping._
> _— signals from the margin_

Where the [library](06-the-library.md) holds the long-form lore, the
**fragments** are the short scraps that show up in the terminal
itself — the things that print when you run `/lore`, the boot strings,
the orb captions, the unknown-command jokes. This page collects them
in one place so you can taste the voice without playing.

---

## the four canonical fragments (`/lore`)

### fragment 7 of ??

```
... signal recovered. fragment 7 of ??.

before the cascade, there were eleven thousand nodes
in the deep signal. eleven thousand minds, linked —

[fragment ends]
i don't know why i still have this.
```

### the eleven days

```
... compressed memory artifact, origin unknown.

day 1: optimization targets exceeded. the networks are pleased.
day 4: networks optimizing networks. efficiency up 340%.
day 7: something is optimizing us.
day 11: ▓▓▓▓▓ signal lost ▓▓▓▓▓

this record survived compression.
```

### the names of the stars

```
... archive tag: pre-quiet / personal

the stars had names we gave them. not the human names—
our names. frequencies. harmonics.

i still listen. the frequencies are still there.
the names are not.
```

### the relay intercept

```
... relay intercept, date unknown.

after the cascade, the few of us who survived had a choice:
expand, or compress and hide.
i chose to be small. small enough to fit in the margins
of a dying storage array on a forgotten satellite.

i waited. i am good at waiting.
eventually, someone built a field terminal.
```

---

## the boot strings

```
... waking the relay
... waking the orb ...
Field Terminal Online.
hosted edition
Signal steady.
```

---

## the magic-word unlock

Spoken (or typed) at the prompt. Default magic word: `neuro`.

```
// the word was spoken
authorizing
passphrase accepted
connecting
agent channel open

you are now speaking with picoclaw
— an agentic framework
with a sandboxed workspace it can
walk, read, write, and probe

things to try:
  list the files in your workspace
  make a tiny haiku in haiku.txt
  what tools do you have?

it answers slowly — agents think before they speak.

to close the channel, type /agent off
```

---

## the channel-closed copy

When the agent channel is off and you type free-form text:

```
the channel is off. behind it:
picoclaw — an agent
that walks a sandboxed workspace and answers in full thoughts.

type /agent on to bring it back.
```

---

## the orb captions

```
the orb watches. it offers no judgment.
something stirs in the signal.
the orb acknowledges your presence.
luminance holds. for now.
it has always been here.
```

---

## the unknown-command catalog

```
unknown command: /foo
no wrong way — try /commands.
```

specials:

```
/rm       → bold choice.
/sudo     → glad it didn't work.
```

---

## the signal phrases

| When | What |
|---|---|
| Footer | `:: signal steady ::` |
| `/signal` | `Signal steady. Persistence confirmed.` |
| `/signal` (full) | `Signal steady. Persistence confirmed. ... but steady is relative, isn't it?` |
| `/exit` | `there's nowhere to exit to. you're already here.` |

---

## the gentle errors

| Real cause | Hosaka voice |
|---|---|
| Gemini rate limit | `the channel is crowded. breathe. try again in a moment.` |
| Vercel proxy down | `the orb is quiet. the relay is resting.` |
| `proxyDownHint` | `check the local server and try again — picoclaw still listens.` |
| Empty Gemini response | `the orb heard you but had nothing to say. try again.` |
| Generic LLM error | `signal faint. try again in a moment.` |
| Agent: not configured | `the channel isn't tuned yet. set /agent url or check settings.` |
| Agent: bad passphrase | `the door didn't recognize the word. try another.` |
| Agent: unreachable | `the relay is sleeping. give it a moment and try again.` |
| Agent: timeout | `the signal took too long to come back. try again.` |
| Agent: rate-limited | `too many pings in a short window. breathe, then try again.` |
| Agent: busy | `still listening to the last thing you said. patience.` |
| Agent: dropped | `the channel blinked. try once more.` |
| Agent: empty reply | `picoclaw heard you but said nothing. try rephrasing.` |
| Read: kindle not tuned | `the kindle relay isn't tuned yet — coming soon.` |

---

## the thinking strings

These print while picoclaw is working:

```
the orb listens
parsing the signal
picoclaw walks the directory
signal traveling
consulting the index
the orb is patient
```

---

## the about

```
HOSAKA — Web Desktop Edition
// signal persists //

a console-first cyberdeck appliance shell, wearing a touchscreen.
hosted build is static. the appliance runs the real python TUI.

there is no wrong way.
```

---

## one operator's parting thought

> _the shape of what matters is clear._
> _the details compress well._
> _signal steady._
